import { IconButton } from '@material-ui/core'
import AddIcon from '@material-ui/icons/Add'

const ICONBUTTON_MUI = () => {
    return (
        <IconButton>
            <AddIcon />
        </IconButton>
    )
}

export default ICONBUTTON_MUI