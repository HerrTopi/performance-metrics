import { Button } from '@material-ui/core'
import AddIcon from '@material-ui/icons/Add'

const BUTTON_ICON_MUI = () => {
    return (
        <Button startIcon={<AddIcon />}>
            asd
        </Button>
    )
}

export default BUTTON_ICON_MUI