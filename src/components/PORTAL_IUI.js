import { Portal } from '@instructure/ui'

const PORTAL_IUI = () => {
    return (
        <Portal open>
            <div>Greetings from the portal!</div>
        </Portal>
    )
}

export default PORTAL_IUI