import { Tooltip } from '@instructure/ui'

const TOOLTIP_IUI = () => {

  return (
      <Tooltip renderTip="Hello. I'm a tool tip" isShowingContent={true}>
        Trigger
      </Tooltip>
  )

}

export default TOOLTIP_IUI
