import { Selectable } from '@instructure/ui'

const SELECTABLE_IUI = () => {

  return (
      <Selectable>{() => {
        return <span>Selectable</span>
      }}</Selectable>
  )

}

export default SELECTABLE_IUI
